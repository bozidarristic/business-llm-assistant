# Example Commands

Index the data:

```bash
python -m app.scripts.index_data
```

Ask questions:

```bash
python -m app.scripts.ask "What is the current status of Beta Manufacturing?"
```

```bash
python -m app.scripts.ask "Summarize all support tickets for Acme Retail."
```

```bash
python -m app.scripts.ask "Which leads should sales prioritize?"
```

```bash
python -m app.scripts.ask "Draft a professional reply to Beta Manufacturing about the SLA issue."
```

```bash
python -m app.scripts.ask "What does the refund policy say?"
```
